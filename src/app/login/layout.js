export function generateMetadata() {
  return {
    title: 'Login - RouteYou',
    description: 'Login with your RouteYou account.',
    keywords: 'login, RouteYou, account',
  };
}

export default async function EventsPage({ children }) {
  return children;
}
