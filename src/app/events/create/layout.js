export function generateMetadata() {
  return {
    title: 'Create Event - RouteYou',
    description: 'Create an event on RouteYou.',
    keywords: 'RouteYou, event, create, create event',
  };
}

export default async function CreateEventLayout({ children }) {
  return children;
}
