export function generateMetadata() {
  return {
    title: 'Register - RouteYou',
    description: 'Create an account on RouteYou.',
    keywords: 'register, create, account, profile',
  };
}

export default async function EventsPage({ children }) {
  return children;
}
